<%- include('../partials/header') %>
<%- include('../partials/navbar') %>

<div class="container">
    <h2>All Orders</h2>
    <ul>
        <% orders.forEach(order => { %>
            <li>
                <span class="label">User ID:</span> <%= order.userId %> | <span class="label">Total:</span> ₹<%= order.total %> | <span class="label">Status:</span> <%= order.status %>
            </li>
        <% }) %>
    </ul>
</div>

<%- include('../partials/footer') %>